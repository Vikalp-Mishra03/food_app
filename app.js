import React from "react";
import ReactDOM from "react-dom/client";

// JSX
const head = (
    <h1 id="title">Hello Everynan</h1>
);

const root = ReactDOM.createRoot(document.getElementById("root"))
root.render(head)
